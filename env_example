# Configuración del Servidor y Base de Datos
PORT=3001
NODE_ENV=development
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/lexis_bill

# Seguridad (OBLIGATORIO - la app no arranca sin JWT_SECRET)
JWT_SECRET=tu_secreto_super_seguro_minimo_32_caracteres
# Producción: origen(es) permitidos para CORS. Múltiples: https://a.com,https://b.com
CORS_ORIGIN=http://localhost:3000

# Dev: URL del API para proxy (rewrites next.config)
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Membresías manuales (Transferencia / PayPal)
LEXISBILL_BANK_NAME=Banco Popular Dominicano
LEXISBILL_BANK_ACCOUNT=XXX-XXXXXX-X
LEXISBILL_PAYPAL_EMAIL=pagos@lexisbill.com

# Crear primer admin: en MongoDB, actualizar usuario: db.users.updateOne({email:"admin@ejemplo.com"}, {$set:{role:"admin"}})

# Monitoreo de errores (OBLIGATORIO en producción)
# Obtén el DSN en: sentry.io → tu proyecto → Settings → Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=https://xxx@o123456.ingest.sentry.io/7890123
# Slug de tu organización y proyecto en Sentry (ej: lexis-bill)
SENTRY_ORG=lexis-bill
SENTRY_PROJECT=javascript-nextjs
# SENTRY_AUTH_TOKEN=  (opcional: para source maps; créalo en sentry.io → Settings → Auth Tokens)

# Backup (scripts/backup-mongodb.js)
BACKUP_OUTPUT_DIR=./backups
# BACKUP_BUCKET=s3://bucket-name (opcional, para subir a S3/R2)

# Pasarelas de Pago (Integración Futura)
# Azul
AZUL_MERCHANT_ID=
AZUL_API_KEY=
AZUL_ENV=sandbox

# Carnet
CARNET_MERCHANT_ID=
CARNET_API_KEY=

# PayPal (Suscripciones)
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_MODE=sandbox

# Soporte (WhatsApp)
# Número con código de país, sin + ni espacios (ej: 18095550000)
NEXT_PUBLIC_SUPPORT_WHATSAPP=18095550000

# APIs Fiscales (DGII)
# Alanube
ALANUBE_API_KEY=
ALANUBE_ENDPOINT=https://api.alanube.co/v1

# FacturaDirecta
FACTURA_DIRECTA_API_KEY=
FACTURA_DIRECTA_ENDPOINT=
